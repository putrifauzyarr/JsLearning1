<!DOCTYPE html>
<html>
  <head>
    <title>Error</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-50 p-8">
    <div class="max-w-2xl mx-auto mt-20">
      <div class="bg-white rounded-lg shadow-md p-6">
        <div class="text-center">
          <h1 class="text-4xl font-bold text-gray-800 mb-4"><%= message || 'Error' %></h1>
          <h2 class="text-2xl text-gray-600 mb-6">Status: <%= error.status || 500 %></h2>
          <p class="text-gray-700 mb-6"><%= error.message || 'An error occurred' %></p>
          <% if (error.status === 403) { %>
            <p class="text-gray-600 mb-6">You don't have permission to access this resource.</p>
          <% } %>
          <% if (error.status === 404) { %>
            <p class="text-gray-600 mb-6">The page you're looking for doesn't exist.</p>
          <% } %>
          <div class="flex gap-4 justify-center">
            <a href="/" class="bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 px-6 rounded-lg transition duration-200">
              Go Home
            </a>
            <% if (error.status === 403 || error.status === 401) { %>
              <a href="/login" class="bg-gray-500 hover:bg-gray-600 text-white font-semibold py-2 px-6 rounded-lg transition duration-200">
                Login
              </a>
            <% } %>
          </div>
          <% if (typeof error.stack !== 'undefined' && process.env.NODE_ENV === 'development') { %>
            <pre class="mt-8 p-4 bg-gray-100 rounded-lg text-left text-xs overflow-auto"><%= error.stack %></pre>
          <% } %>
        </div>
      </div>
    </div>
  </body>
</html>