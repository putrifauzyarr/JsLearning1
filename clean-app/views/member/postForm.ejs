<!DOCTYPE html>
<html>
  <head>
    <title><%= isEdit ? 'Edit Post' : 'Create New Post' %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-50 p-8">
    <div class="max-w-3xl mx-auto">
      <!-- Header -->
      <div class="mb-6">
        <a href="/dashboard" class="text-blue-500 hover:text-blue-600 mb-4 inline-block">← Back to My Posts</a>
        <h1 class="text-3xl font-bold text-gray-800"><%= isEdit ? 'Edit Post' : 'Create New Post' %></h1>
      </div>

      <!-- Form -->
      <div class="bg-white rounded-lg shadow-md p-6">
        <% if (typeof error !== 'undefined' && error) { %>
          <div class="mb-4 p-3 bg-red-100 border border-red-400 text-red-700 rounded-lg">
            <%= error %>
          </div>
        <% } %>

        <form action="<%= isEdit ? '/dashboard/posts/' + post.id : '/dashboard/posts' %>" method="POST" class="space-y-6">
          <div>
            <label for="title" class="block text-sm font-medium text-gray-700 mb-2">Title:</label>
            <input 
              type="text" 
              id="title" 
              name="title" 
              required
              value="<%= post ? post.title : '' %>"
              class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none transition duration-200"
              placeholder="Enter post title"
            >
          </div>

          <div>
            <label for="content" class="block text-sm font-medium text-gray-700 mb-2">Content:</label>
            <textarea 
              id="content" 
              name="content" 
              required
              rows="15"
              class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none transition duration-200"
              placeholder="Write your post content here..."><%= post ? post.content : '' %></textarea>
          </div>

          <div class="flex gap-4 pt-4">
            <button 
              type="submit"
              class="bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 px-6 rounded-lg transition duration-200 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2"
            >
              <%= isEdit ? 'Update Post' : 'Create Post' %>
            </button>
            <a 
              href="/dashboard"
              class="bg-gray-200 hover:bg-gray-300 text-gray-700 font-semibold py-2 px-6 rounded-lg transition duration-200 focus:outline-none focus:ring-2 focus:ring-gray-400 focus:ring-offset-2 inline-block text-center"
            >
              Cancel
            </a>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>